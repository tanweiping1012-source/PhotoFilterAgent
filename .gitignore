engine/.build/
bench/out/
# 不写斜杠：符号链接形式的 node_modules（本机把它指到 worktree 那份）不是目录，
# `node_modules/` 匹配不到它，2026-09-18 就差点被 `git add -A` 兜进提交。
node_modules
.DS_Store

# 照片一律不进版本库 —— 仓库是公开的。
# 2026-09-03 出过一次：ask-AB.jpg / ask-BA.jpg 两张给标注者看的对比图
# 带着可辨认的人脸被提交并推上了公开仓库。图本身留在本地，只是不再跟踪。
*.jpg
*.jpeg
*.JPG
*.JPEG
*.png
*.heic
*.HEIC
*.tif
*.tiff
*.dng
*.raf
