# 阶段 3 端到端 A/B · 第四轮 · 报告

判据 [CRITERIA-E2E.md](CRITERIA-E2E.md)（含修订 1–5），**写在开跑之前、跑完一个字没改**。
本文的每个数字都能从 `~/.dsh-v4/photo-filter-v4/archive/round4-e2e/runs/*/run/` 的运行记录重算出来。

> ⏳ **状态：数据未齐。** 已完成 **8 次有效**（第一、二轮各四组），A-3 进行中。
> 下面的结构与口径已经定死（呈现方式在看到数据之前就写好了），数字随运行补入。

---

## 0. 先说三件与实验结论无关、但更值得记的事

### 0.1 产品缺陷：web 会话的阶段 2 从来没发过锚点

第一次冒烟花了 121 次调用才看见：**web 会话用的是 preset 那一份配置**，而 `anchorsFile` 与
`allowNeither` 此前只写在 profile 里。于是 web 里的阶段 2 一直裸跑 ——
一张范例照片都没发、也不允许模型回答「两张都不够格」—— 而张数、名单、指纹、调用数**全部正常**。

headless 不读 preset，那一路 profile 是活的，所以**这个缺陷只打 web**。

**仓库已修**（`87e142c`：preset 补上两个键，并把 `stage2Vlm: true` 显式写出来）；
**部署的那份实验期间不动** —— 改部署件会让已经跑完的运行不可比。
实测部署的五份 preset md5 全程未变（基线 `71172d70`、四份分组 `954546b2` / `38551222` / `ea9077b6` / `fabe6843`），
四份分组与基线的 diff 始终是纯追加 9 行。**全部跑完后再部署**（见 §5 收尾清单）。

一份意外的对照数据（两种配置，不是同一条件下的前后对比，**不要读成「锚点让模型更稳」**）：

| 配置 | 阶段 2 双向一致率 | 「两张都不够格」 |
|---|---|---|
| 无锚点 + allowNeither 关（作废那次） | 17/60 = 28% | 结构上不可能出现 |
| 带 10 张锚点 + allowNeither 开（A-1） | 25/60 = 42% | 6 次 |

两个数都在自己的 60% 验收线以下。

### 0.2 守卫的模型是错的，所以它对这次事故全程是绿的

`check_preset_shadow.py` 正是 2026-09-05 同类事故（废掉 120 次调用）之后写的，
但它写着「preset 与 profile 逐键合并、preset 覆盖」，于是只报「两边都写了、值不同」的键。

实际机制是 preset 自己挂一棵子树：**preset 没写的键落到插件 schema 的默认值，不回落到 profile**。
09-05 是这个模型的前一半，09-18 是后一半。**仓库已修**（`87e142c`），两类都报，并已变异验证；
同样地，部署件实验期间不动。

⚠️ 同时更正一个过强的说法：**profile 那棵子树并没有消失**。只有它才有的额外工具照常注册 ——
实测第三轮标定的 web 会话里，calib-web 的 `evalPairsFile` 让 `run_pair_eval` 出现在 9 个可见工具里，
而基线 preset 全文没有任何 `evalPairs*` 键，所以那两个工具只可能来自 profile 那个实例。

**观察与机制要分开写。** 我们证到的是「实测如此」：
同名工具（`rank_photos` 等）生效的是 preset 那份 config —— 这是从作废那次的 `run.json` **反推**出来的
（`anchorsFile` 落到 schema 默认值，而不是 profile 写的路径），**不是从 loader 代码里读出来的**。
「谁覆盖谁」的机制本轮没有从代码核实。这一轮已经因为「从 createdAt 推上下文」错过一次，
凡是推出来的都标明是推出来的。

### 0.3 分组必须落在 preset 上

因为上面这条机制，四份按 profile 分组的 E2E profile 里，阶段 3 的三个键**全是死的** ——
照原计划跑下去，四组会跑成同一组，2700 次调用全部白花。改为四份 preset 分组（判据修订 5.1）。

---

## 1. 这一轮在测什么

在真实的 299 张候选池里跑**产品路径**（web UI → `rank_photos`），四组只差阶段 3 的开关与输入：

| 组 | 阶段 2 | 阶段 3 | 阶段 3 的提示 |
|---|---|---|---|
| A | 开（完整产品） | **关** | — |
| B1 | 开 | 开 | 无 |
| B2 | 开 | 开 | 跨场景 rubric（1154 字 · `e018ae40…`） |
| B3 | 开 | 开 | rubric + 8 张范例锚点（`b44c32d8…`） |

每组 5 次，轮流跑（A→B1→B2→B3 为一轮）。主指标是**交付 20 张 ∩ 用户精选 20 张**。

**这个结构测得出「会不会变坏」，测不出「有没有变好」**：每次运行的对局计划里通常只有
1~2 个「上行局」（挑战者是金标），而下行局有 4~5 个。每次的可达区间由算分脚本现算并列出。

---

## 2. 怎么读这些数字（呈现规则，定在看到数据之前）

1. **净变化逐次列出**，5 次不合并成一个数（判据 §5.2b）
2. 合并数只作**描述性汇总，不给置信区间** —— 5 次里大量是同一对照片重复问，不是独立样本（修订 2.7）
3. 净变化 **< 0 的次数**决定发布文档的措辞档位（修订 2.4）；> 0 的次数同时报，**不称为「提升」**
4. 每次同时列**可达区间**与**上行局在哪几段** —— 计划里没有上行局的那次，+1 根本够不着
5. A 与 B 的主指标**只并列摆出**，差值混着阶段 2 的噪声（修订 1.4）
6. 决定性对局**逐对报**：这一对出现 n 次、判中 x 次、未表态 y 次（修订 2.7 主报法）

---

## 3. 逐次运行

> 待补：全部运行跑完后由 `e2e/score_e2e.py` 输出，owner 独立复算，两边对上才写进来。

### 第一轮（四组各 1 次 —— **不能比**，见 3.1）

```
组   交付₂ 交付₃ 净变化 换人 决定性 判中/有方向 未表态 可达  一致率  计划 md5
A-1    7    7    +0    0    5      0/0       0    3~8   —      576c87be
B1-1   6    7    +1    1    6      3/3       3    2~8   6/10   37e21431
B2-1   7    6    −1    1    5      2/3       2    2~7   5/10   43fed439
B3-1   7    8    +1    2    6      3/3       3    2~8   4/10   0c2486c7
```

### 第二轮（进行中）

```
组   交付₂ 交付₃ 净变化 换人 决定性 判中/有方向 未表态 可达  一致率  计划 md5
A-2    5    5    +0    0    4      0/0       0    2~6   —      df2eb3bc
B1-2   6    6    +0    3    6      2/3       3    2~8   6/10   8f557106
       段 2：DSCF9081 → DSCF9089（中性）
       段 7：DSCF9423 → DSCF9406（换进金标，+1）
       段 9：DSCF9533 → DSCF9528（换出金标，−1）
B2-2   6    7    +1    3    5      4/5       —    3~8   8/10   1a439d4a
       换进 DSCF9111、DSCF9406（**两张都是金标**），换出 DSCF9533（金标）→ 净 +1
B3-2   6    5    −1    2    4      1/2       —    2~6   4/10   4313573a
       换出 DSCF8896（**金标**）与 DSCF9453，换进 DSCF9013、DSCF9505（都不是金标）→ 净 −1
```

**阶段 3 的双向一致率在 40~80% 之间波动（n=10 局），本报告不做解释。**
一度想说「阶段 3 图少反而更一致」，不成立：20 幅那次（B3-1）反而是最低的 4/10，
而 4 幅的四次自己就从 5/10 跳到 8/10 —— n=10 的二项噪声足以解释。
且阶段 2 比的是同一组连拍（几乎一样的两张），阶段 3 比的是不同场景，**任务本身就不同**。

### 两轮之后：该看的量是**同组内主指标的跨度**，不是名单重合

一度想写「同配置名单重合 16~17/20，而组间差都在 ±1，所以净变化落在噪声里」。
**名单重合和净变化不是同一个量**（执行方指出）：重合 16/20 只说明有 4 张照片不同，
那 4 张里可能一张金标都没有 —— B1 就是这样，两次名单重合 18，而两次金标命中都是 6，跨度 0。

要支撑那句话，该用的是**同一组重复跑时主指标自己的跨度**：

```
组   交付₂ 金标   交付₃ 金标   跨度（交付₃）    名单重合（交付₂ / 交付₃）
A    7, 5        7, 5        **2**           16 / 16     ← 阶段 3 关，这是纯阶段 2 的噪声
B1   6, 6        7, 6        1               18 / 16
B2   7, 6        6, 7        1               16 / 17
B3   7, 6        8, 5        **3**           18 / 15
对照：八次运行的阶段 3 净变化全部落在 −1 ~ +1
```

⚠️ **纯噪声带宽只能看 A 组**（阶段 3 关着），目前是 2。B3 的跨度 3 混了阶段 2 噪声
与阶段 3 两次相反的换人（+1 与 −1），不能直接当噪声读。

还有一条只当观察：**阶段 3 让同组两次的名单更不像，而不是更像** ——
B1 交付₂ 18 → 交付₃ 16、B3 18 → 15，只有 B2 反着（16 → 17）。n=3 组。

换人逐局（都是**双向一致**的判断，不是翻覆蒙上的）：

```
B1-1  段 7  DSCF9423 → DSCF9406（金标）            +1
B2-1  段 9  DSCF9533（金标） → DSCF9528            −1   两个方向都稳定选了非金标那张
B3-1  段 0  DSCF8832 → DSCF8886                    0
      段 7  DSCF9423 → DSCF9406（金标）            +1
```

**同一组两次的差，已经有实测幅度了**：A-1 7/20、A-2 5/20，两次交付名单重合 **16/20**
（只在 A-1 里的金标 DSCF9111、DSCF9379；只在 A-2 里的金标没有）。阶段 3 在 A 组是关的，
所以这 2 张全部来自阶段 2 的噪声 —— **同配置下 20 张里能差 4 张、主指标能差 2 张**。
任何小于这个幅度的组间差都读不出东西来，这是读下面所有数字的尺子。

### 3.1 计划稳定性

阶段 2 的噪声每次会换掉约 4 个段的对局，所以**每次运行的对局表都不一样**：

```
组     上行局的段   下行局的段      可达    与冻结对局表相同
A-1    [7]         [1,3,6,9]      3~8     7/10
B1-1   [6,7]       [1,3,5,9]      2~8     6/10
B2-1   （无）       [1,3,5,6,9]    2~7     7/10
B3-1   [7]         [1,3,5,6,9]    2~8     7/10
```

**B2 那次为什么没有上行局，查到了具体原因** —— 不是「运气差」：

```
A-1 / B1-1 / B3-1   段 7 = DSCF9423（在位） vs DSCF9406（挑战者，**金标**）
B2-1                段 7 = DSCF9426（在位） vs DSCF9423（挑战者，非金标）
```

阶段 2 的判决把 DSCF9423 从「在位」挪成了「挑战者」，金标 DSCF9406 根本没进那一局。

顺带一条逐对事实：`DSCF9423 vs DSCF9406` 这一对，在阶段 3 真判过的两次（B1-1、B3-1）里
**两次都判了金标那张**。A-1 的计划里也有它，但 A 组阶段 3 关着，没判。

**没有上行局的那次，+1 结构上够不着。** B2-1 就是这种：它的可达区间是 2~7，
净变化最好也只能是 0 —— 所以 B1 的 +1 与 B2 的 −1 **不是同一条件下的对比**。
这两个数并排出现时必须带上这一行，否则会被读成「rubric 起了反作用」。

### 3.1b 同一对照片，不同运行里未必判成一样

一度以为「模型对具体某一对的判断跨运行相当稳定，净变化只取决于计划凑到了哪几对」。
**前半句不成立**（执行方用全表推翻，owner 独立重建同一张表、逐行一致）。

阶段 3 真判过的**六次**里，出现 ≥2 次的对共 12 对，
**每次答案都相同的只有 3 对**：

```
DSCF9423 vs DSCF9406（金标在乙）   b × 4              四次都判金标
DSCF9325 vs DSCF9324（金标在甲）   a × 3
DSCF9325 vs DSCF9337（金标在甲）   inconsistent × 2   ← 仅存的「稳定判不出来」
```

**「稳定判不出来」也不稳定。** `DSCF9453 vs DSCF9505` 的六次履历：

```
inconsistent · inconsistent · inconsistent · inconsistent · neither · **b**
```

连翻四次、第五次答「两张都不够格」、第六次给出决定性答案并真的换了人。

⚠️ 这张表**每加一次运行就缩水**：四次运行时「4 对相同」，五次变 3 对（稳定翻覆从 2 对降到 1 对），
六次仍是 3 对但成员变了。**稳定的那部分只会随样本增加而变小。**

⚠️ 这张表**跨了 B1/B2/B3 三种提示条件**，所以只能当观察摆出来，不能当逐对指标 ——
修订 2.7 的逐对报是**组内**的。

**成立的那半句**：净变化取决于这次的计划凑到了哪几对，而那些对**本身多数不稳定**。
B1-2 是个好例子：换人 3 局（段 2 中性、段 7 换进金标、段 9 换出金标），净变化正好抵消成 0。

### 3.2 报废与 5xx：视觉调用这条路**没有重试覆盖**

到 B2-2 为止一共 **14 次尝试：7 次有效、7 次失败**
（其中 1 次是 0 调用就被挡在门外，6 次有沉没调用）。
失败里三次是外部故障：两次 529（`overloaded_error`）、一次 `terminated`（流被切断）。

一度以为「LLM 层默认 5 次重试都用尽了，纯属服务端问题」。**这个结论是错的**，执行方推翻并给了硬证据：

- 重试执行器 `@deepseek-ai/dsh-llm-retry` 的模块说明写明它挂在**agent loop 的请求恢复扩展点**上
  （`inject = ['agents']`）；`retryPolicy` 只是挂在 provider 注册信息里的策略对象
- 而视觉调用是 `harness-vision.ts` 自己 `llm.prepareCall(...)` + `prepared.stream(...)`，**不经过 agent loop**
- 实测：两次失败的会话里 `llm/retry` **各 0 条**；B2 那次失败调用 `elapsed_ms = 1599`，
  而同一次运行里成功调用的中位数是 4525 ms —— 默认退避 500+1000+2000+4000+8000 = 15.5 秒，
  1.6 秒装不下 6 次尝试。**那次只试了一次**
- B3 那次 `elapsed_ms = 75780`（单次在服务端挂住后失败），同样 0 条重试

**耗时要看分布，不要看极值，也别设固定阈值**。十一次运行的实测：

```
中位      2.6 ~ 5.5 秒
p90       5.6 ~ 11.7 秒
最大      8.4 ~ 51.7 秒      超过 30 秒的：只有 3 次运行各出现 1~2 次
失败那几次 1 秒（429）· 2 秒（529 快失败）· 76 秒（529 慢失败）· **249 秒（terminated）**
```

**中位与 p90 稳定，最大值是偶发离群** —— 只报极值会让人以为这条链路普遍很慢。
只有 249 秒那次明显落在分布之外。

`terminated` 这条尤其站得住：适配器**就是把流中断归到 `TRANSPORT`** 的
（`llm-deepseek/src/adapter.ts:498`、`llm-pi-ai/src/stream.ts:55/62`），而 `TRANSPORT` 在默认可重试集里，
那次会话的 `llm/retry` 同样是 0 条。

**三次外部失败是三种形态（SERVER × 2、TRANSPORT × 1），全部落在同一个缺口上。**

（严格口径：证到的是「没有执行到那条策略」，没有逐层读 provider adapter。
另外「成功调用中位数」一律只算**比较调用、不含预检** —— 含预检会得到不同的数。）

所以报废归因是。**「证到的」与「观察到的」分成两列** ——
原先合成一列「性质」，把「重试能不能救回来」这种**观察不到的推测**写了进去，
而且自相矛盾（`terminated` 挂 249 秒标成「本可重试」，529 挂 76 秒却标成「外部」，
同一种形态两个标签）。执行方指出后改成：

| 次 | 原因 | 花费 | 是否走到重试（**证到的**） | 服务端表现（**观察到的**） |
|---|---|---|---|---|
| A-1 首次 | preset 挂错，阶段 2 没发锚点 | 121 | 不适用 | 配置错，已变成守卫 |
| B2 第二次 | 旧会话里重发，主动掐断 | 18 | 不适用 | 流程错，已变成守卫 |
| B2 第三次 | 529 | 33 | **否**（路径无覆盖，`llm/retry` 0 条） | 快拒，1.6 秒 |
| B3 第一次 | 529 | 9 | **否**（同上） | 挂死，76 秒 |
| A-2 第一次 | `terminated` 流被切断 | 56 | **否**（同上） | 挂死，249 秒 |
| B2-2 第一次 | 429 Token Plan 用尽 | 74 | 不适用 | 额度，1 秒 |

**「重试能不能救回来」观察不到**，只作为「后续改进」的假设，不进归因表。

**作废有三种形态，报告里不要混**：

```
门外被挡      0 次调用，**连运行记录目录都没有** → 只能在账本里手记（B2 首次尝试那回）
跑到一半      有沉没调用（目前 6 次、沉没 311 次）→ 算分脚本逐次列出
发调用前被拦   有记录、但守卫在第一次调用前就停住（本轮还没出现过）
```

**后续改进的首选项因此变了**：原来写的是「一次失败让整段阶段 2 作废 → 改成这一对记未表态并继续」；
现在更靠前的是**先把重试接到视觉路径上**（或在 `comparePairs` 里对 5xx 做有限重试）——
它比改判决语义小得多，也不动任何口径。

⚠️ **本轮不改**：改重试或改容错都会让 A-1 / B1-1 / B2-1 已经跑过的语义不可比。

---

## 4. 成本

```
有效  A-1 121 · B1-1 142 · B2-1 142 · B3-1 142
      A-2 121 · B1-2 142 · B2-2 142 · B3-2 142                    = 1094
报废  121（preset 挂错）· 18（旧会话重发，掐断）· 33（529）
      · 9（529）· 56（terminated）· 74（429 跑到一半）      = 311
———————————————————————————————————————————————————————————————
已花                                                       1405
```

⚠️ **报废损耗要单列，不能只给理论值**：目前 **8 次有效 / 7 次失败**，报废 311 次
（沉没 293 + 主动掐断 18）。剩余 12 次运行的理论成本约 1642 次；
按目前的报废比例，**实际总花费可能接近理论值的两倍**。

口径（修订 4.2）：数的是**真发出去的调用**，含路由预检，从 `calls.jsonl` 的 `sent` 行数出；
与 `run.json` 的 `comparisons + preflights` 必须逐次对上（修订 4.4）。

⚠️ 「视觉调用数」与「Token Plan 消耗」是两个口径：agent 自己的文本请求（含重试）
也吃同一份额度，但不进 `calls.jsonl`。

---

## 5. 发布

无论结果如何，阶段 3 都以**默认关闭的可选开关**发布（`stage3Vlm`，默认 `false`）。
实测数字只决定文档怎么写、推不推荐开（措辞档位见判据修订 2.4）。

> 待补：文档措辞与 README 的开启说明，数据齐了再写。

**收尾清单（全部跑完之后）**：
1. 部署 `87e142c` 修好的 preset（web 会话的阶段 2 补上锚点与 allowNeither）
2. 把 preset 的临时排除（10 张锚点）恢复，并记第三条 md5
3. 删掉四份分组 preset `photo-filter-v4-e2e-*`
4. README 写明怎么开 `stage3Vlm`（默认关），措辞按修订 2.4 的档位

---

## 6. 这一轮改了哪些代码

| commit | 内容 |
|---|---|
| `d561a16` | 排序器接上阶段 3：出计划与计划 md5，带着它回来应用裁决 |
| `b622dda` | `rank_photos` 接上阶段 3（默认关），阶段 2/3 运行记录独立落盘，锚点泄题拦截 |
| `5d7740c` | 执行方独立复审的 6 处：失败路径的调用记账、判官按结果算、两处退出码口子、锚点字段、A 组也记计划、交付路径不留共享文件 |
| `a436155` | 锚点记账分开张数与幅数 |
| `12bf3eb` | `rank_photos` 闭包的接线行为测试（真 index.ts + 假排序器，0 次调用） |
| `e516e71` | 逐次运行的离线重算 + 算分脚本跟上判据修订 4 |
| `43426e0` | 会话检查必须指名本组 preset |
| `2cbd144` | 算分报计划稳定性（上行局在不在、与冻结表差多少） |
| `87e142c` | web 会话阶段 2 补上锚点与 allowNeither；preset 影子守卫改成认得出这种形态 |
| `8ca3629` | 算分：一致率 × 换人 × 净变化三列（只列不解读） |
| `f342da6` | 算分：比较调用的耗时分布、作废运行逐次记账 |
| `1ce1635` | 算分：跳过还没收尾的运行（目录开跑时建、`run.json` 收尾才写） |
| `b7ee7f6` | 算分：同组交付名单两两重合 —— 读组间差的尺子 |
| `0e44cdd` | 算分：耗时报 p90 与慢调用次数；作废分「跑到一半」与「没花钱」 |
| `ff7c8b0` | 算分：阶段 3 没跑到时不再报「rubric md5 不符」—— 那是假警报 |
